package aula;

// enumerated = enumerado (listável)
enum Categoria {
    // contantes são uppercase
    // A ORDEM IMPORTA, passeio = 0, utilitário = 1
    PASSEIO, UTILITARIO;
    // passeio, utilitário;
}
